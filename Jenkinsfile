pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls'
                sh 'pwd'
                sh 'docker --version'
                sh 'echo "Nick"'
            }
        }
    }
}
